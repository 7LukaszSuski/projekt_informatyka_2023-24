#pragma once
#include <SFML/Graphics.hpp>
using namespace sf;
class Bullet {
public:
	Bullet(Texture* texture, Vector2f pos);
	Sprite shape;
};

class Bullet2 {
public:
	Bullet2(Texture* texture, Vector2f pos);
	Sprite shape;
};
